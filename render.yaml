# databases:
#   - name: fas_r_db
#     plan: free
#     databaseName: plutohr_db
#     user: fas_r_db

services:
  - type: web
    plan: free
    name: fas_lms_backend
    runtime: python3.12
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python manage.py runserver "    #envVars:
      # - key: DATABASE_URL
      #   fromDatabase:
      #     name: plutohr_db
      #     property: connectionString
      # - key: SECRET_KEY
      #   generateValue: true
      - key: WEB_CONCURRENCY
        value: 4